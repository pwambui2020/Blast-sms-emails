openapi: 3.0.0

info:
  title: Blast Hub API
  version: 1.0.0
  description: API for the Blast Hub SMS and Email platform.

servers:
  - url: http://localhost:5000/api/v1

paths:
  /health:
    get:
      summary: Check API health
      responses:
        "200":
          description: API is healthy